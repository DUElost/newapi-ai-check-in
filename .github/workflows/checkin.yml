name: newapi.ai 自动签到
on:
  schedule:
    - cron: '0 */8 * * *'
  workflow_dispatch:
    inputs:
      accounts:
        description: '账号配置 (JSON 格式，可选，不填则使用 secrets.ACCOUNTS)'
        required: false
        type: string
        default: ''
      providers:
        description: '自定义 Provider 配置 (JSON 格式，可选，不填则使用 secrets.PROVIDERS)'
        required: false
        type: string
        default: ''
      debug:
        description: '调试模式 (启用后会保存截图和 HTML 日志)'
        required: false
        type: boolean

jobs:
  checkin:
    permissions:
      id-token: write
      contents: read
    runs-on: windows-2025
    environment: production
    env:
      PYTHONIOENCODING: utf-8
    steps:
    - name: set beijing timezone
      uses: szenius/set-timezone@v2.0
      with:
        timezoneWindows: "China Standard Time"

    - uses: actions/checkout@v4

    - uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 缓存 UV 依赖
      uses: actions/cache@v4
      id: uv-cache
      with:
        path: |
          ~/.cache/uv
          .venv
        key: ${{ runner.os }}-uv-${{ hashFiles('pyproject.toml', 'uv.lock') }}
        restore-keys: |
          ${{ runner.os }}-uv-

    - name: 安装依赖
      if: steps.uv-cache.outputs.cache-hit != 'true'
      run: |
        echo "缓存未命中，开始安装项目依赖..."
        uv sync
        echo "✅ 环境初始化完成"

    - name: 获取 Camoufox 版本
      id: camoufox-version
      run: |
        $version = uv run python -c "import importlib.metadata; print(importlib.metadata.version('camoufox'))"
        echo "version=$version" >> $env:GITHUB_OUTPUT
        echo "Camoufox version: $version"

    - name: 缓存 Camoufox 浏览器
      uses: actions/cache@v4
      id: camoufox-cache
      with:
        path: ~\AppData\Local\camoufox
        key: ${{ runner.os }}-camoufox-${{ steps.camoufox-version.outputs.version }}
        restore-keys: |
          ${{ runner.os }}-camoufox-

    - name: 安装 Camoufox 浏览器
      if: steps.camoufox-cache.outputs.cache-hit != 'true'
      run: |
        echo "缓存未命中，开始下载 Camoufox 浏览器..."
        uv run camoufox fetch
        echo "✅ Camoufox 浏览器安装完成"

    - name: 恢复登录状态缓存
      uses: actions/cache/restore@v4
      with:
        path: |
          storage-states
        key: storage-state-${{ hashFiles('storage-states/*.json') }}
        restore-keys: |
          storage-state-

    - name: 恢复余额历史缓存
      uses: actions/cache/restore@v4
      with:
        path: |
          balance_hash.txt
        key: balance-hash-${{ hashFiles('balance_hash.txt') }}
        restore-keys: |
          balance-hash-

    - name: 组装配置（支持非 JSON Secret）
      env:
        ACCOUNTS_RAW: ${{ inputs.accounts || secrets.ACCOUNTS }}
        ACCOUNTS_LINUX_DO_RAW: ${{ secrets.ACCOUNTS_LINUX_DO }}
        ACCOUNTS_GITHUB_RAW: ${{ secrets.ACCOUNTS_GITHUB }}
        PROVIDERS_RAW: ${{ inputs.providers || secrets.PROVIDERS }}
        PROXY_RAW: ${{ secrets.PROXY }}
        ANYROUTER_SESSION_1: ${{ secrets.ANYROUTER_SESSION_1 }}
        ANYROUTER_API_USER_1: ${{ secrets.ANYROUTER_API_USER_1 }}
        ANYROUTER_NAME_1: ${{ secrets.ANYROUTER_NAME_1 }}
        ANYROUTER_PROVIDER_1: ${{ secrets.ANYROUTER_PROVIDER_1 }}
        CHECKIN_LINUX_DO_USERNAME: ${{ secrets.CHECKIN_LINUX_DO_USERNAME }}
        CHECKIN_LINUX_DO_PASSWORD: ${{ secrets.CHECKIN_LINUX_DO_PASSWORD }}
        CHECKIN_GITHUB_USERNAME: ${{ secrets.CHECKIN_GITHUB_USERNAME }}
        CHECKIN_GITHUB_PASSWORD: ${{ secrets.CHECKIN_GITHUB_PASSWORD }}
      run: |
        function Export-EnvVar {
          param(
            [Parameter(Mandatory = $true)][string]$Name,
            [AllowNull()][AllowEmptyString()][string]$Value
          )
          if ([string]::IsNullOrWhiteSpace($Value)) {
            return
          }
          Add-Content -Path $env:GITHUB_ENV -Value "$Name<<EOF"
          Add-Content -Path $env:GITHUB_ENV -Value $Value
          Add-Content -Path $env:GITHUB_ENV -Value "EOF"
        }

        function Normalize-JsonArrayOrEmpty {
          param(
            [Parameter(Mandatory = $true)][string]$Name,
            [AllowNull()][AllowEmptyString()][string]$Value,
            [bool]$AllowEmptyArray = $true
          )
          if ([string]::IsNullOrWhiteSpace($Value)) {
            return ""
          }
          try {
            $parsed = $Value | ConvertFrom-Json -Depth 100
            if (-not ($parsed -is [System.Array])) {
              Write-Host "⚠️ $Name 不是 JSON 数组，已忽略该值"
              return ""
            }
            if (-not $AllowEmptyArray -and $parsed.Count -eq 0) {
              Write-Host "ℹ️ $Name 是空数组，视为未配置"
              return ""
            }
            return ($parsed | ConvertTo-Json -Compress -Depth 100)
          } catch {
            Write-Host "⚠️ $Name 不是合法 JSON，已忽略该值: $($_.Exception.Message)"
            return ""
          }
        }

        $accounts = Normalize-JsonArrayOrEmpty -Name "ACCOUNTS_RAW" -Value $env:ACCOUNTS_RAW -AllowEmptyArray $false
        if ([string]::IsNullOrWhiteSpace($accounts)) {
          $hasSession = -not [string]::IsNullOrWhiteSpace($env:ANYROUTER_SESSION_1)
          $hasApiUser = -not [string]::IsNullOrWhiteSpace($env:ANYROUTER_API_USER_1)

          if ($hasSession -and $hasApiUser) {
            $accountName = if ([string]::IsNullOrWhiteSpace($env:ANYROUTER_NAME_1)) { "账号1" } else { $env:ANYROUTER_NAME_1 }
            $provider = if ([string]::IsNullOrWhiteSpace($env:ANYROUTER_PROVIDER_1)) { "anyrouter" } else { $env:ANYROUTER_PROVIDER_1 }
            $accounts = @(
              @{
                name = $accountName
                provider = $provider
                cookies = @{
                  session = $env:ANYROUTER_SESSION_1
                }
                api_user = $env:ANYROUTER_API_USER_1
              }
            ) | ConvertTo-Json -Compress -Depth 20
            Write-Host "✅ 已通过 ANYROUTER_SESSION_1 + ANYROUTER_API_USER_1 自动生成 ACCOUNTS"
          } elseif ($hasSession -or $hasApiUser) {
            Write-Host "::error::ANYROUTER_SESSION_1 与 ANYROUTER_API_USER_1 必须同时配置"
            exit 1
          }
        }

        if ([string]::IsNullOrWhiteSpace($accounts)) {
          Write-Host "::error::未提供 ACCOUNTS，且未提供可自动组装的 ANYROUTER_SESSION_1/ANYROUTER_API_USER_1"
          exit 1
        }
        Export-EnvVar -Name "ACCOUNTS" -Value $accounts

        $linuxDoAccounts = Normalize-JsonArrayOrEmpty -Name "ACCOUNTS_LINUX_DO_RAW" -Value $env:ACCOUNTS_LINUX_DO_RAW
        if ([string]::IsNullOrWhiteSpace($linuxDoAccounts)) {
          $hasLinuxUser = -not [string]::IsNullOrWhiteSpace($env:CHECKIN_LINUX_DO_USERNAME)
          $hasLinuxPass = -not [string]::IsNullOrWhiteSpace($env:CHECKIN_LINUX_DO_PASSWORD)
          if ($hasLinuxUser -and $hasLinuxPass) {
            $linuxDoAccounts = @(
              @{
                username = $env:CHECKIN_LINUX_DO_USERNAME
                password = $env:CHECKIN_LINUX_DO_PASSWORD
              }
            ) | ConvertTo-Json -Compress -Depth 10
            Write-Host "✅ 已通过 CHECKIN_LINUX_DO_USERNAME/PASSWORD 自动生成 ACCOUNTS_LINUX_DO"
          } elseif ($hasLinuxUser -or $hasLinuxPass) {
            Write-Host "::error::CHECKIN_LINUX_DO_USERNAME 与 CHECKIN_LINUX_DO_PASSWORD 必须同时配置"
            exit 1
          }
        }
        Export-EnvVar -Name "ACCOUNTS_LINUX_DO" -Value $linuxDoAccounts

        $githubAccounts = Normalize-JsonArrayOrEmpty -Name "ACCOUNTS_GITHUB_RAW" -Value $env:ACCOUNTS_GITHUB_RAW
        if ([string]::IsNullOrWhiteSpace($githubAccounts)) {
          $hasGithubUser = -not [string]::IsNullOrWhiteSpace($env:CHECKIN_GITHUB_USERNAME)
          $hasGithubPass = -not [string]::IsNullOrWhiteSpace($env:CHECKIN_GITHUB_PASSWORD)
          if ($hasGithubUser -and $hasGithubPass) {
            $githubAccounts = @(
              @{
                username = $env:CHECKIN_GITHUB_USERNAME
                password = $env:CHECKIN_GITHUB_PASSWORD
              }
            ) | ConvertTo-Json -Compress -Depth 10
            Write-Host "✅ 已通过 CHECKIN_GITHUB_USERNAME/PASSWORD 自动生成 ACCOUNTS_GITHUB"
          } elseif ($hasGithubUser -or $hasGithubPass) {
            Write-Host "::error::CHECKIN_GITHUB_USERNAME 与 CHECKIN_GITHUB_PASSWORD 必须同时配置"
            exit 1
          }
        }
        Export-EnvVar -Name "ACCOUNTS_GITHUB" -Value $githubAccounts

        Export-EnvVar -Name "PROVIDERS" -Value $env:PROVIDERS_RAW
        Export-EnvVar -Name "PROXY" -Value $env:PROXY_RAW

    - name: 校验账号配置 JSON
      run: |
        function Test-JsonType {
          param(
            [Parameter(Mandatory = $true)]
            [string]$Name,
            [Parameter(Mandatory = $true)]
            [string]$ExpectedType,
            [bool]$Required = $false
          )

          $value = [Environment]::GetEnvironmentVariable($Name)
          if ([string]::IsNullOrWhiteSpace($value)) {
            if ($Required) {
              Write-Host "::error::$Name 未配置，请在 production 环境 Secret 中设置"
              exit 1
            }
            Write-Host "ℹ️ $Name 未配置，跳过校验"
            return $null
          }

          try {
            $parsed = $value | ConvertFrom-Json -Depth 100
          } catch {
            Write-Host "::error::$Name 不是合法 JSON: $($_.Exception.Message)"
            exit 1
          }

          if ($ExpectedType -eq "array" -and -not ($parsed -is [System.Array])) {
            Write-Host "::error::$Name 必须是 JSON 数组格式 []"
            exit 1
          }
          if ($ExpectedType -eq "object" -and ($parsed -is [System.Array] -or $null -eq $parsed)) {
            Write-Host "::error::$Name 必须是 JSON 对象格式 {}"
            exit 1
          }

          Write-Host "✅ $Name JSON 格式校验通过"
          return $parsed
        }

        function Has-Property {
          param(
            [Parameter(Mandatory = $true)]$Obj,
            [Parameter(Mandatory = $true)][string]$Name
          )
          return $null -ne $Obj.PSObject.Properties[$Name]
        }

        $accounts = Test-JsonType -Name "ACCOUNTS" -ExpectedType "array" -Required $true
        if ($accounts.Count -eq 0) {
          Write-Host "::error::ACCOUNTS 不能为空数组，至少需要一个账号"
          exit 1
        }

        for ($i = 0; $i -lt $accounts.Count; $i++) {
          $account = $accounts[$i]
          if ($null -eq $account.PSObject) {
            Write-Host "::error::ACCOUNTS[$i] 必须是 JSON 对象"
            exit 1
          }

          $hasCookies = Has-Property -Obj $account -Name "cookies"
          $hasLinuxDo = Has-Property -Obj $account -Name "linux.do"
          $hasGithub = Has-Property -Obj $account -Name "github"

          if (-not ($hasCookies -or $hasLinuxDo -or $hasGithub)) {
            Write-Host "::error::ACCOUNTS[$i] 必须包含 cookies / linux.do / github 其中之一"
            exit 1
          }

          if ($hasCookies -and -not (Has-Property -Obj $account -Name "api_user")) {
            Write-Host "::error::ACCOUNTS[$i] 使用 cookies 时必须配置 api_user"
            exit 1
          }
        }

        $linuxDoAccounts = Test-JsonType -Name "ACCOUNTS_LINUX_DO" -ExpectedType "array"
        if ($linuxDoAccounts) {
          for ($i = 0; $i -lt $linuxDoAccounts.Count; $i++) {
            $item = $linuxDoAccounts[$i]
            if (-not (Has-Property -Obj $item -Name "username") -or -not (Has-Property -Obj $item -Name "password")) {
              Write-Host "::error::ACCOUNTS_LINUX_DO[$i] 必须包含 username 和 password"
              exit 1
            }
          }
        }

        $githubAccounts = Test-JsonType -Name "ACCOUNTS_GITHUB" -ExpectedType "array"
        if ($githubAccounts) {
          for ($i = 0; $i -lt $githubAccounts.Count; $i++) {
            $item = $githubAccounts[$i]
            if (-not (Has-Property -Obj $item -Name "username") -or -not (Has-Property -Obj $item -Name "password")) {
              Write-Host "::error::ACCOUNTS_GITHUB[$i] 必须包含 username 和 password"
              exit 1
            }
          }
        }

        Test-JsonType -Name "PROVIDERS" -ExpectedType "object" | Out-Null

        $proxyValue = [Environment]::GetEnvironmentVariable("PROXY")
        if ([string]::IsNullOrWhiteSpace($proxyValue)) {
          Write-Host "ℹ️ PROXY 未配置，跳过校验"
        } else {
          try {
            $proxyObj = $proxyValue | ConvertFrom-Json -Depth 100
            if ($proxyObj -is [System.Array] -or $null -eq $proxyObj) {
              Write-Host "::error::PROXY 为 JSON 时必须是对象格式 {}"
              exit 1
            }
            Write-Host "✅ PROXY JSON 对象格式校验通过"
          } catch {
            # 兼容项目原有行为：PROXY 可直接使用字符串代理地址
            Write-Host "ℹ️ PROXY 不是 JSON，将按纯字符串代理地址使用"
          }
        }

    - name: 账号配置预览（脱敏）
      run: |
        function Parse-JsonOrNull {
          param(
            [Parameter(Mandatory = $true)]
            [string]$Name
          )

          $value = [Environment]::GetEnvironmentVariable($Name)
          if ([string]::IsNullOrWhiteSpace($value)) {
            return $null
          }

          try {
            return $value | ConvertFrom-Json -Depth 100
          } catch {
            Write-Host "ℹ️ $Name 无法解析为 JSON，预览时跳过"
            return $null
          }
        }

        function Has-Property {
          param(
            [Parameter(Mandatory = $true)]$Obj,
            [Parameter(Mandatory = $true)][string]$Name
          )
          return $null -ne $Obj.PSObject.Properties[$Name]
        }

        $accounts = Parse-JsonOrNull -Name "ACCOUNTS"
        if ($null -eq $accounts -or -not ($accounts -is [System.Array])) {
          Write-Host "ℹ️ ACCOUNTS 不可预览"
          exit 0
        }

        Write-Host "📊 脱敏预览摘要:"
        Write-Host "  - ACCOUNTS 总数: $($accounts.Count)"

        for ($i = 0; $i -lt $accounts.Count; $i++) {
          $account = $accounts[$i]
          $displayName = if ((Has-Property -Obj $account -Name "name") -and -not [string]::IsNullOrWhiteSpace($account.name)) { $account.name } else { "Account $($i + 1)" }
          $provider = if ((Has-Property -Obj $account -Name "provider") -and -not [string]::IsNullOrWhiteSpace($account.provider)) { $account.provider } else { "anyrouter" }
          $hasCookies = Has-Property -Obj $account -Name "cookies"
          $hasApiUser = Has-Property -Obj $account -Name "api_user"
          $hasLinuxDo = Has-Property -Obj $account -Name "linux.do"
          $hasGithub = Has-Property -Obj $account -Name "github"

          Write-Host "  - [$i] name=$displayName, provider=$provider, cookies=$hasCookies, api_user=$hasApiUser, linux.do=$hasLinuxDo, github=$hasGithub"
        }

        $linuxDoAccounts = Parse-JsonOrNull -Name "ACCOUNTS_LINUX_DO"
        if ($linuxDoAccounts -is [System.Array]) {
          Write-Host "  - ACCOUNTS_LINUX_DO 总数: $($linuxDoAccounts.Count)"
        } else {
          Write-Host "  - ACCOUNTS_LINUX_DO: 未配置"
        }

        $githubAccounts = Parse-JsonOrNull -Name "ACCOUNTS_GITHUB"
        if ($githubAccounts -is [System.Array]) {
          Write-Host "  - ACCOUNTS_GITHUB 总数: $($githubAccounts.Count)"
        } else {
          Write-Host "  - ACCOUNTS_GITHUB: 未配置"
        }

        $providers = Parse-JsonOrNull -Name "PROVIDERS"
        if ($null -ne $providers -and -not ($providers -is [System.Array])) {
          Write-Host "  - PROVIDERS 自定义数量: $($providers.PSObject.Properties.Count)"
        } else {
          Write-Host "  - PROVIDERS: 未配置"
        }

        $proxy = [Environment]::GetEnvironmentVariable("PROXY")
        if ([string]::IsNullOrWhiteSpace($proxy)) {
          Write-Host "  - PROXY: 未配置"
        } else {
          try {
            $proxyObj = $proxy | ConvertFrom-Json -Depth 100
            if ($proxyObj -is [System.Array] -or $null -eq $proxyObj) {
              Write-Host "  - PROXY: 已配置（类型异常）"
            } else {
              $proxyServer = if ($null -ne $proxyObj.PSObject.Properties["server"] -and -not [string]::IsNullOrWhiteSpace($proxyObj.server)) { "已设置 server" } else { "无 server 字段" }
              Write-Host "  - PROXY: 已配置（JSON 对象, $proxyServer）"
            }
          } catch {
            Write-Host "  - PROXY: 已配置（字符串）"
          }
        }
             
    - name: 执行签到
      env:
        DEBUG: ${{ github.event_name == 'workflow_dispatch' && inputs.debug || vars.DEBUG || 'false' }}
        DINGDING_WEBHOOK: ${{ secrets.DINGDING_WEBHOOK }}
        EMAIL_USER: ${{ secrets.EMAIL_USER }}
        EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
        EMAIL_TO: ${{ secrets.EMAIL_TO }}
        CUSTOM_SMTP_SERVER: ${{ secrets.CUSTOM_SMTP_SERVER }}
        PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
        SERVERPUSHKEY: ${{ secrets.SERVERPUSHKEY }}
        FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        WEIXIN_WEBHOOK: ${{ secrets.WEIXIN_WEBHOOK }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        # 列出 storage-states 目录文件
        if (Test-Path "storage-states") {
          echo "📁 storage-states 目录内容:"
          echo "绝对路径: $(Resolve-Path storage-states)"
          echo ""
          echo "所有文件:"
          Get-ChildItem -Path storage-states -File | ForEach-Object { echo "  - $($_.Name)" }
          echo ""
          echo "详细信息:"
          Get-ChildItem -Path storage-states -File | Format-Table Name, Length, LastWriteTime -AutoSize
          echo "文件总数: $((Get-ChildItem -Path storage-states -File).Count)"
        } else {
          echo "⚠️ storage-states 目录不存在"
        }
        # 执行签到
        uv run python -u main.py

    - name: 保存登录状态缓存
      if: hashFiles('storage-states/*.json') != ''
      uses: actions/cache/save@v4
      with:
        path: |
          storage-states
        key: storage-state-${{ hashFiles('storage-states/*.json') }}

    - name: 保存余额历史缓存
      if: hashFiles('balance_hash.txt') != ''
      uses: actions/cache/save@v4
      with:
        path: |
          balance_hash.txt
        key: balance-hash-${{ hashFiles('balance_hash.txt') }}

    - name: 保存日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: artifacts-${{ github.run_number }}
        path: |
          logs/
          screenshots/
        if-no-files-found: ignore
        retention-days: 7

    - name: 执行结果
      if: always()
      run: |
        echo "签到任务执行完成"
        echo "时间: $(Get-Date)"
